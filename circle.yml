machine:
  python:
    version: 2.7.5
  environment:
    DJANGO_SETTINGS_MODULE: settings.test

dependencies:
  pre:
    - pip install -r meancoach_project/requirements/test.pip

test:
  override:
    - (cd meancoach_project && flake8 *.py)
    - (cd meancoach_project && py.test)

